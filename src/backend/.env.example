# =========================================
# Database Configuration - Events DB
# Implements database integration layer requirements
# =========================================
EVENTS_DB_HOST=localhost
EVENTS_DB_PORT=5432
EVENTS_DB_USER=events_user
EVENTS_DB_PASSWORD=events_password
EVENTS_DB_NAME=events_db

# =========================================
# Database Configuration - Inventory DB
# Implements database integration layer requirements
# =========================================
INVENTORY_DB_HOST=localhost
INVENTORY_DB_PORT=5432
INVENTORY_DB_USER=inventory_user
INVENTORY_DB_PASSWORD=inventory_password
INVENTORY_DB_NAME=inventory_db

# =========================================
# API Configuration
# Implements API integration layer requirements
# =========================================
GRAPHQL_ENDPOINT=http://localhost:4000/graphql
GRAPHQL_AUTH_TOKEN=your_graphql_auth_token
REST_BASE_URL=http://localhost:3000/api
REST_AUTH_TOKEN=your_rest_auth_token

# =========================================
# Logging Configuration
# Implements component configuration requirements
# =========================================
LOG_LEVEL=info
LOG_FILE_PATH=./logs

# =========================================
# System Configuration
# Implements component configuration requirements
# =========================================
# Environment setting (development, staging, production)
NODE_ENV=development

# Server configuration
PORT=3000

# API client settings
API_TIMEOUT=5000
API_RETRY_ATTEMPTS=3
API_RETRY_BACKOFF=1000

# Database pool configuration
DB_POOL_SIZE=10
DB_IDLE_TIMEOUT=10000
DB_CONNECTION_TIMEOUT=5000

# Note: Replace sensitive values with appropriate secrets in production
# For production deployments, use secure secrets management solutions